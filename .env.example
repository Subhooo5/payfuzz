# Stripe webhook signing secret, shared by the payfuzz signer and both victims.
#
# Local development: paste the output of `stripe listen --print-secret`.
# CI / offline:      any consistent value works (payfuzz signs deliveries and
#                    the victims verify them with this same secret).
PAYFUZZ_WHSEC=whsec_replace_me
